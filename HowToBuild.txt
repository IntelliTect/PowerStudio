
If you are not using Visual Studio 2010 to build the VSIX package, you can use the psake build scripts.

If you want to build the VSIX and run the unit tests, Double click on the build-release.cmd file.

If you want to build the VSIX without running the unit tests, Double click on the build.cmd file.
